/* A integer to Roman conversion. */

/* Accepts an integer (u8).
Converts the integer to Roman literal and outputs it.
*/

// a function that takes in an integer and returns 

pub fn convert(num: &u8) -> String {
    
    if name == "Marco" {
        "Polo".to_string()
    } else {
        "Marco".to_string()
    }
}